import 'package:flutter/material.dart';

/// Tonic app color palette - Victorian Apothecary aesthetic.
/// Rich, warm colors optimized for sleep/focus with luxurious feel.
class TonicColors {
  TonicColors._();

  // Base palette - deep espresso tones
  static const base = Color(0xFF0F0D0B);
  static const surface = Color(0xFF1C1816);
  static const surfaceLight = Color(0xFF2A2420);
  static const surfaceElevated = Color(0xFF352E28);

  // Accent palette - antique gold
  static const accent = Color(0xFFD4A855);
  static const accentLight = Color(0xFFE8C77B);
  static const accentDark = Color(0xFF9A7830);
  static const accentMuted = Color(0xFF8B7340);

  // Cream highlights for labels and decorative elements
  static const cream = Color(0xFFFAF6EF);
  static const creamMuted = Color(0xFFE8E0D4);
  static const creamDark = Color(0xFFD4C9B8);

  // Text colors
  static const textPrimary = Color(0xFFF5EFE6);
  static const textSecondary = Color(0xFFB8AFA0);
  static const textMuted = Color(0xFF7A7268);
  static const textAccent = Color(0xFFD4A855);

  // Safety indicator colors (volume levels)
  static const safe = Color(0xFF5C8A6B);
  static const safeLight = Color(0xFF7AAD8A);
  static const moderate = Color(0xFFD4A855);
  static const moderateLight = Color(0xFFE8C77B);
  static const warning = Color(0xFFBF6B4A);
  static const warningLight = Color(0xFFD4896B);

  // Tonic signature colors - refined
  static const brightTonic = Color(0xFFF0EAE0);
  static const brightTonicGlow = Color(0xFFFFFCF5);
  static const restTonic = Color(0xFFD4899A);
  static const restTonicGlow = Color(0xFFE8A8B8);
  static const focusTonic = Color(0xFFA68520);
  static const focusTonicGlow = Color(0xFFC9A040);

  // Botanical colors - nature-inspired
  static const rainBotanical = Color(0xFF7A9DAD);
  static const oceanBotanical = Color(0xFF3D7A8C);
  static const windBotanical = Color(0xFF8A9CAD);

  // Decorative colors
  static const border = Color(0xFF3D352E);
  static const borderLight = Color(0xFF4A4138);
  static const divider = Color(0xFF2A2420);
  static const overlay = Color(0x80000000);
  static const glassBorder = Color(0x30F5EFE6);
  static const glassHighlight = Color(0x15FFFFFF);

  // Gradient stops
  static const gradientDark = Color(0xFF0A0908);
  static const gradientMid = Color(0xFF1C1816);
  static const gradientLight = Color(0xFF2A2420);
}
